# -----------------------------------------------------------------------------
# Fichier de configuration de l'application batch
# -----------------------------------------------------------------------------
login    =  @batchUserLogin@
password =  @batchUserPassword@

host = @serverHost@
port = @serverPort@

artifactId = mint-server
artifactId2 = @artifactId@

one-project-property=une valeur
my.basedir=./target
